package com.com.aditechnology.stateinforamtioncenter.repositary

import com.com.aditechnology.stateinforamtioncenter.model.State

class GetStateDetailsUseCase(private val repository: StateRepository) {
    /*suspend operator fun invoke(state: String): State {
       // return repository.getStateDetails(state)
        return
    }*/
}
